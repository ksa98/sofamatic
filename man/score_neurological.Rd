% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/score_neurological.R
\name{score_neurological}
\alias{score_neurological}
\title{Neurological component of the SOFA score}
\usage{
score_neurological(gcs, sedation = NULL, na_strategy = c("na", "zero"))
}
\arguments{
\item{gcs}{Integer or numeric vector of GCS values (3–15).}

\item{sedation}{Optional logical or 0/1 vector indicating whether the
patient was sedated at the time of assessment. When \code{NULL}, missing GCS
values produce \code{NA_integer_} scores.}

\item{na_strategy}{How to handle \code{NA} GCS when \code{sedation} is \code{NULL}. One
of \code{"na"} (default — return \code{NA_integer_}) or \code{"zero"} (treat missing
GCS as the best state, score 0).}
}
\value{
Integer vector in \verb{\{0, 1, 2, 3, 4\}} (or \code{NA_integer_}).
}
\description{
Scores the neurological subscale of the SOFA from the Glasgow Coma Scale
(GCS). When \code{sedation} is supplied, a missing GCS in a sedated patient is
interpreted as moderate impairment (score 2), whereas a missing GCS in a
non-sedated patient is treated as the best clinical state (score 0).
This reflects the common ICU practice in which a fully responsive,
non-sedated patient is often not re-assessed and so the GCS field is
left blank.
}
\section{Scoring rules}{

\itemize{
\item GCS 15:        0
\item GCS 13–14:     1
\item GCS 10–12:     2
\item GCS  6–9:      3
\item GCS  < 6:      4
}
}

\examples{
score_neurological(c(15, 13, 11, 8, 4, NA))
score_neurological(c(15, 13, NA, NA),
                   sedation = c(0, 0, 1, 0))

}
