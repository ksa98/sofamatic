% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calculate_sofa.R
\name{calculate_sofa}
\alias{calculate_sofa}
\title{Calculate SOFA scores for a clinical dataset}
\usage{
calculate_sofa(
  data,
  pao2_fio2 = "pao2_fio2",
  ventilated = "ventilated",
  platelets = "platelets",
  bilirubin = "bilirubin",
  vasopressors = "vasopressors",
  noradrenaline = "noradrenaline",
  dopamine = NULL,
  dobutamine = NULL,
  epinephrine = NULL,
  map = "map",
  sedation = "sedation",
  gcs = "gcs",
  creatinine = "creatinine",
  urine_output = "urine_output",
  dialysis = "dialysis",
  id = NULL,
  time = NULL,
  units_pao2_fio2 = c("kPa", "mmHg"),
  units_bilirubin = c("umol/L", "mg/dL"),
  units_creatinine = c("umol/L", "mg/dL"),
  na_strategy = c("sum_na_zero", "propagate")
)
}
\arguments{
\item{data}{A data frame in long format. Each row is one observation
(one subject, one time point).}

\item{pao2_fio2, platelets, bilirubin, map, gcs, creatinine}{Column names (as
character strings) holding the corresponding clinical measurements.}

\item{ventilated, sedation, vasopressors, dialysis}{Optional column names
for binary indicators (logical or 0/1). Pass \code{NULL} to skip a variable.}

\item{noradrenaline, dopamine, dobutamine, epinephrine}{Optional column names
for vasoactive drug doses (ug/kg/min). Pass \code{NULL} to skip a drug.}

\item{urine_output}{Optional column name for 24-hour urine output (mL).}

\item{id, time}{Optional column names for subject id and time. When both
are supplied, the returned data frame includes \code{SOFA_admission} (the
\code{SOFA_score} at the earliest \code{time} per \code{id}) and \code{SOFA_max} (the maximum
\code{SOFA_score} per \code{id}).}

\item{units_pao2_fio2}{\code{"kPa"} or \code{"mmHg"}.}

\item{units_bilirubin, units_creatinine}{\code{"umol/L"} or \code{"mg/dL"}.}

\item{na_strategy}{How to aggregate the six subscores into a total. One of
\code{"sum_na_zero"} (default -- \code{NA} subscores contribute 0 to the total,
matching \code{rowSums(..., na.rm = TRUE)}) or \code{"propagate"} (any \code{NA}
subscore yields \code{NA} total).}
}
\value{
A data frame: \code{data} augmented with columns \code{SOFA_resp},
\code{SOFA_coag}, \code{SOFA_liver}, \code{SOFA_cardio}, \code{SOFA_neuro}, \code{SOFA_renal},
\code{SOFA_score}, plus \code{SOFA_admission} and \code{SOFA_max} if \code{id}/\code{time} are
supplied.
}
\description{
\code{calculate_sofa()} is the workhorse of the package. It takes a data frame
(cross-sectional or longitudinal in long format) and appends the six SOFA
subscores plus the total \code{SOFA_score} column. For longitudinal data
(one row per subject per day), supply \code{id} and optionally \code{time} to also
get \code{SOFA_admission} (first observation) and \code{SOFA_max} (worst score
during follow-up) per subject.
}
\details{
Default column names match the parameter names so a data frame with
columns \code{pao2_fio2}, \code{platelets}, \code{bilirubin}, \code{map}, \code{gcs}, \code{creatinine},
etc. works out of the box. Override any of them for datasets that use
different naming conventions.
}
\examples{
df <- example_sofa_data()
out <- calculate_sofa(df, id = "subject_id", time = "day")
head(out[, c("subject_id", "day",
             "SOFA_resp", "SOFA_coag", "SOFA_liver",
             "SOFA_cardio", "SOFA_neuro", "SOFA_renal",
             "SOFA_score", "SOFA_admission", "SOFA_max")])

}
